<%= form_for(parkingevent) do |f| %>
  <% if parkingevent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(parkingevent.errors.count, "error") %> prohibited this parkingevent from being saved:</h2>

      <ul>
      <% parkingevent.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :parkrequest %>
    <%= f.datetime_select :parkrequest %>
  </div>

  <div class="field">
    <%= f.label :parkallowed %>
    <%= f.datetime_select :parkallowed %>
  </div>

  <div class="field">
    <%= f.label :parkcompleted %>
    <%= f.text_field :parkcompleted %>
  </div>

  <div class="field">
    <%= f.label :getbikecompleted %>
    <%= f.datetime_select :getbikecompleted %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
