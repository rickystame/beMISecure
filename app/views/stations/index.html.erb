<p id="notice"><%= notice %></p>
 <div id="leafletMap"></div>

  <%= javascript_tag do %>
    var leafletMap= L.map('leafletMap').setView([45.463683,9.190619],10);
    L.tileLayer('http://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
          maxZoom: 19,
          attribution: '&copy; <a href="http://www.openstreetmap.org/copyright">OpenStreetMap</a>'
        }).addTo(leafletMap);
    // icon 
    var greenIcon= L.icon({
    iconUrl: '<%= asset_path "rails.png" %>',
    iconSize: [16, 36], // size1 of the icon
    iconAnchor: [8, 18], // point of the icon which will correspond to marker's location
    popupAnchor: [0, 0] // point from which the popup should open relative to the iconAnchor
        });
    var marker= new Array();
    <% @stations.each_with_index do |station, contatore| %>

      marker[0] = L.marker([<%= station.lat %>,<%= station.long %>], {icon: greenIcon}).addTo(leafletMap);
      marker[0].bindPopup('<b><%= station.station_name %></b>')
    
      
    <% end %>
     <% end %>
<h1>STAZIONI</h1>

<table>
  <thead>
    <tr>
      <th>latitudine</th>
      <th>longitudine</th>
      <th>indirizzo</th>
      <th>numero catena</th>
      <th>numero stazione</th>
      <th>catene libere</th>
      <th>nome stazione</th>
      <th>armadietti</th>
      <th>sicurezza</th>
      <th>livello sicurezza></th>
    </tr>
  </thead>

  <tbody>
    <% @stations.each do |station| %>
      <tr>
        <td><%= station.lat %></td>
        <td><%= station.long %></td>
        <td><%= station.address %></td>
        <td><%= station.number_chains %></td>
        <td><%= station.station_number %></td>
        <td><%= station.free_chains %></td>
        <td><%= station.station_name %></td>
        <td><%= station.lockers %></td>
        <td><%= station.security %></td>
        <td><%= link_to 'Show', station %></td>
        <td><%= link_to 'Edit', edit_station_path(station) %></td>
        <td><%= link_to 'Destroy', station, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Station', new_station_path %>
